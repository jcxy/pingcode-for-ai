{
  "name": "@cvtoolman/pingcode-cli",
  "version": "0.2.1",
  "description": "PingCode CLI - 通过命令行操作 PingCode，配合 Skill 文件为 AI 编程工具提供项目管理能力",
  "type": "module",
  "main": "dist/client/index.js",
  "types": "dist/client/index.d.ts",
  "bin": {
    "pingcode-cli": "dist/cli/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "fast-check": "^3.15.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
